# Getting Started 🔱  
### Install, Initialize, and Begin Using the Trident CLI

This guide walks you through installing Trident, verifying your environment, and running your first sovereign command.

---

## 🔱 Installation

Trident requires:

- Python 3.10+
- Git
- A POSIX‑compatible shell (Linux, macOS, WSL)

Install via pip:

```bash
pip install trident-cli
